# @package _global_
# DEHB search: no bonus, so only the two shared knobs. If every config scores 0 the
# landscape is flat and the incumbent is arbitrary; the trial history is the result.
defaults:
  - _base
  - override /bonus: none
  - _self_
